{
  "name": "careflow-ai",
  "version": "1.0.0",
  "private": true,
  "description": "CareFlow AI - Healthcare Referral & Prior Authorization Intelligence Platform",
  "scripts": {
    "build": "npm run build --prefix frontend",
    "dev": "npm run dev --prefix frontend",
    "lint": "npm run lint --prefix frontend"
  }
}
